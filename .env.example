# OpenAI API Key (optional - will use rule-based responses if not provided)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_api_key_here

# Server Port (optional - defaults to 3000)
PORT=3000
